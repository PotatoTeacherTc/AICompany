# AICompany Architecture

## Current execution flow

```text
Task → TaskQueue → TaskWorker → Manager → TaskClassifier
     → PipelineRegistry → BasePipeline implementation → PipelineResult
     → Task status/result → ExecutionHistory → HistoryAnalyzer
```

`main.py` builds the default registry, creates Tasks from input strings, adds
them to TaskQueue, and invokes TaskWorker. Importing `main` does not execute
the workflow; `run()` is the explicit entry point.

## Implemented layers

| Layer | Current modules | Responsibility and dependency |
|---|---|---|
| Task model | `core/task.py`, `core/status.py`, `core/result.py` | Task owns ID, text, optional structured parameters, an optional parent Task ID, lifecycle timestamps, result, task type, pipeline, bounded retry metadata, and an optional timeout. It exposes PENDING/QUEUED/RUNNING/SUCCESS/FAILED/SKIPPED/CANCELLED/TIMED_OUT transitions. PipelineResult serializes the common result fields. |
| Application service | `application/automation_service.py` | AutomationService is a framework-independent composition boundary for injected Manager, ExecutionHistory, ArtifactManager, TaskQueue, and TaskWorker dependencies. It creates/submits Tasks and runs work without exposing Pipeline or Repository calls to upper layers. Its task-ID cancel/retry controls use a small in-process lock and the existing Queue retry/cancel paths, preserving one upserted history record per Task. `main.py` uses it while preserving `run()` compatibility. |
| Query service | `application/task_query_service.py` | TaskQueryService converts Task, ExecutionHistory query results, provider usage, and ArtifactManager records into serializable DTO dictionaries. It returns a safe `found: false` response for unknown task IDs and reuses `ExecutionHistory.query()` for filtered lists. |
| User domain | `core/user.py`, `core/user_repository.py`, `application/user_service.py` | UserService creates credential-free User records through injected in-memory or JSON-file repositories. A record contains ID, normalized email, ACTIVE/INACTIVE lifecycle state, and creation/update timestamps; duplicate normalized emails are rejected. Legacy records without lifecycle fields normalize to ACTIVE, while invalid records are ignored safely. Deactivation is idempotent and restart-persistent. Login/current-user checks and new Workspace membership reject inactive Users. Passwords, hashes, sessions, and tokens remain separate from this record. |
| Credential domain | `core/password_hasher.py`, `core/credential_repository.py`, `application/credential_service.py` | CredentialService keeps password verification separate from User records. Its injectable PasswordHasher defaults to salted PBKDF2-SHA256 and stores only `user_id` plus a password hash through in-memory or JSON-file repositories. Plain passwords, tokens, and raw verification errors are not persisted. |
| Login and token domain | `core/access_token_provider.py`, `application/login_service.py` | LoginService composes UserService and CredentialService, returning an access token only after safe credential verification. Its injectable provider defaults to an HMAC-signed, expiry-checked standard-library token with only `user_id` and expiry claims; token values are never stored. |
| Session domain | `core/session_repository.py`, `application/session_service.py` | SessionService stores user-scoped refresh-token hashes through injected in-memory or JSON-file repositories. It creates, rotates, expires, lists, and revokes sessions without exposing refresh-token values or hashes. The FastAPI boundary provides refresh, logout, list, and owner-scoped session deletion endpoints. |
| Audit domain | `core/audit_repository.py`, `application/audit_service.py` | AuditService records safe lifecycle events through injectable in-memory or JSON-file repositories. Its metadata guard excludes passwords, tokens, hashes, prompts, and non-scalar values. Workspace audit queries support action/date filters and paging; authenticated OWNER/ADMIN access is required. |
| Audit query service | `application/audit_query_service.py` | AuditQueryService applies descending audit ordering, workspace-scoped resource/user/action/date filters, and opaque cursor or compatible offset pagination. |
| Request diagnostics | `api/request_context.py`, `api/app.py`, `api/errors.py` | RequestContext uses isolated context-local correlation IDs. Middleware safely reuses valid client IDs, returns the ID in response headers, and sanitized errors include it for support correlation; audit events can store it without credentials or request bodies. |
| Collaboration mission contract | `core/mission.py` | Mission is an immutable, serializable unit-of-work contract containing a unique ID, title, objective, requester identity, explicit workspace ownership, timezone-aware creation time, and isolated extension metadata. MissionState restricts lifecycle changes to PENDING → IN_PROGRESS → terminal, with direct cancellation also allowed. Exclusive lock operations record one owner and timezone-aware acquisition time, return replacement Mission values, and reject other owners without leaking identities. Workspace ownership prevents future collaboration records from becoming tenant-ambiguous. This is an in-memory value contract rather than a repository-backed distributed lock; work execution remains a later integration mission. |
| Mission workspace | `core/mission_workspace.py` | MissionWorkspaceManager creates a `workspace_id/mission_id` directory beneath an injected and resolved root. Identifier validation and descendant checks prevent path traversal. This is the current local worktree-equivalent abstraction; it performs no Git operations and never commits or pushes. |
| Worker context | `core/worker_context.py` | ContextBuilder converts only a validated Mission into a WorkerContext containing Mission and Workspace identity plus the normalized objective and optional isolated Mission workspace path. Metadata is copied through a scalar-only filter that removes credential, token, password, and raw-prompt keys. It does not call providers or execute work. |
| Worker result | `core/worker_result.py` | WorkerResult is a serializable, timezone-stamped terminal outcome tied to one worker, Mission, and Workspace. It reuses terminal PipelineStatus values, the existing provider usage field names, and artifact dictionaries so later ExecutionHistory integration does not require a competing result vocabulary. |
| Collaboration worker | `core/collaboration_worker.py` | BaseWorker defines `execute(WorkerContext) -> WorkerResult`. FunctionWorker supplies the minimum dependency-injected local/fake implementation, validates worker/Mission/Workspace result identity, and converts handler exceptions to type-only FAILED results. It is not a provider worker or orchestrator and performs no worktree or validation lifecycle. |
| Worker result validation | `core/worker_result_validator.py` | WorkerResultValidator returns a small ValidationResult and checks the WorkerResult contract, Mission/Workspace identity, success/error consistency, and artifact workspace ownership without returning submitted values in errors. |
| Provider workers | `core/provider_workers.py` | ProviderWorker converts a WorkerContext objective to the existing ProviderRequest and maps ProviderResponse into WorkerResult. ClaudeWorker and GeminiWorker provide distinct identities over this shared adapter. ProviderFactory's MockProvider remains the default, missing UsageMetadata becomes zeros, request echoes are redacted, and provider/timeout failures disclose only exception types. Elapsed timeout is detected after the provider returns; calls are not forcibly interrupted. |
| Collaboration orchestration | `core/collaboration_orchestrator.py`, `core/execution_history.py` | CollaborationOrchestrator moves a PENDING Mission to IN_PROGRESS, acquires and releases the Mission lock for each unique Worker, builds an isolated Context, executes and validates results, then marks the Mission COMPLETED or FAILED. CollaborationResult exposes only a safe Mission summary. ExecutionHistory records one workspace-scoped COLLABORATION entry containing Worker status/usage/artifact/error summaries but no objective, prompt, metadata, or Worker data. Human approval and Git commit/push are outside this flow. |
| Membership domain | `core/workspace_membership.py`, `core/workspace_membership_repository.py`, `application/workspace_membership_service.py` | WorkspaceMembershipService links existing Users and Workspaces with OWNER, ADMIN, or MEMBER roles through injectable in-memory or JSON-file repositories. It rejects duplicate or dangling memberships and prevents removal or demotion of the last OWNER. It is not an authentication or authorization mechanism. |
| API contracts and application | `api/contracts.py`, `api/task_api.py`, `api/app.py`, `api/errors.py` | Transport-neutral request/response DTOs define task creation plus single/list retrieval. The FastAPI app factory injects application services, exposes task control/query endpoints, workspace create/list/detail endpoints, `POST /users`, `GET /users/{user_id}`, and workspace-membership create/list/role-change/removal endpoints. It returns uniform sanitized 4xx/5xx errors; duplicate identity/membership and final-OWNER conflicts return 409. TaskApi depends only on AutomationService and TaskQueryService, while user/workspace routes depend only on their application services, never repositories or Pipelines. Authentication is not implemented yet. |
| Queue and execution | `core/task_queue.py`, `core/worker.py` | FIFO queue accepts optional ExecutionHistory and retry-limit dependencies. It cancels only queued non-terminal work. Worker records QUEUED, RUNNING, retry, timeout, and terminal updates through the same task ID; History upserts the record so result, timestamps, duration, retry count, timeout, and sanitized error type remain synchronized without duplicates. Current synchronous timeout handling marks an over-deadline execution after its handler returns; it does not interrupt a running handler. |
| Routing | `agent/manager.py`, `agent/classifier.py`, `agent/goal_task_planner.py`, `core/registry.py` | Classifier selects a task type; GoalTaskPlanner converts structured goal steps into parent-linked Tasks only when their task types are registered. Manager preserves that validated declared type (or classifies an untyped Task), resolves it through the registry, and calls one Pipeline. Registry accepts only non-empty task types and BasePipeline implementations, rejects duplicate type registrations, and exposes registered task-type/pipeline/capability metadata for planning. Manager accepts only PipelineResult dictionaries with every required key, an allowed PipelineStatus value, and metadata matching the current Task and selected Pipeline. |
| Pipeline contract | `core/base_pipeline.py` | Each Pipeline receives a Task and returns a PipelineResult dictionary. |
| Implemented pipelines | `core/pipeline.py` (FILE), `music_pipeline.py`, `content_pipeline.py`, `research_pipeline.py`, `history_pipeline.py`, `main.py` (intentional FAIL) | Produce files or history data and return common results. FILE, MUSIC, CONTENT, and RESEARCH accept test output path injection. |
| Persistence and analysis | `core/execution_history.py`, `core/execution_history_repository.py`, `core/history_analyzer.py` | ExecutionHistory keeps its existing record API while receiving an InMemory or JSON repository through DI. Its query API returns latest-first records and supports status, pipeline, task-type, and ISO-date-range filters with offset/limit pagination. HistoryAnalyzer reuses that query contract to aggregate status, pipeline/task-type, and provider/model usage/token/cost data. JSON persistence safely handles missing/corrupt files and uses atomic replacement for writes. |
| Artifact boundary | `core/artifact_manager.py`, `core/artifact_repository.py` | ArtifactManager creates stable artifact IDs, registers file metadata, and exposes artifact lookup/list queries through an injected InMemory or file-backed repository. FILE, MUSIC, CONTENT, and RESEARCH optionally receive it through DI and register their output files without changing their existing `files_created` results. Standard metadata includes ID, type, MIME type, filename, size, creation time, producer pipeline, and a reserved workspace ID. PipelineResult carries those records into ExecutionHistory without changing its existing fields. |
| Supporting services | `agent/planner.py`, `executor.py`, `validator.py`, `reporter.py`, `scripts/` | FILE pipeline planning, execution, validation, report generation, and logging. TaskPlanner produces the execution plan; FILE adds its target folder and TaskExecutor consumes that plan rather than an independent folder argument. |
| Tests | `Automation/tests/test_pipeline_system.py` | Standard-library unittest regression suite using temporary directories. It covers SUCCESS, FAILED, NOT_IMPLEMENTED, exception, registry, and Manager result-contract boundaries. |
| AI provider boundary | `providers/base.py`, `providers/models.py`, `providers/mock_provider.py`, `providers/factory.py`, `providers/pipeline_utils.py` | Provider-neutral request/response and usage metadata. The shared utility normalizes absent or partial usage and formats provider errors without raw messages. MockProvider is the offline default; CONTENT, RESEARCH, and MUSIC can receive an injected provider and persist only provider/model/token/cost metadata through PipelineResult and ExecutionHistory, never API keys. |
| Music provider boundary | `providers/music.py`, `providers/factory.py` | MusicProvider defines `generate_music(MusicGenerationRequest) -> MusicGenerationResult` with generated artifact and optional shared UsageMetadata contracts. FakeMusicProvider is the deterministic offline selection; GenericMusicProviderAdapter keeps existing AIProvider injection compatible. Internal generated artifacts carry paths only for registration, while their default serialization omits paths. No external music service or API key is built in. |
| Music execution and history | `core/music_pipeline.py`, `core/artifact_manager.py`, `core/execution_history.py` | MusicPipeline writes only below its configured `workspace_id` directory, validates Mission scope IDs and provider artifacts, normalizes missing usage, and maps timeout/provider errors to safe PipelineResult values. ArtifactManager retains internal paths in its repository, but Music PipelineResult exposes only standard safe metadata. Optional `record_music` upserts one MUSIC record with workspace, mission, provider, model, status, usage, and safe artifacts; it records neither task text nor absolute paths, and history failures do not override generation status. |
| Image/video provider boundary | `providers/content_media.py`, `providers/factory.py` | Provider-neutral image/video requests and results reuse UsageMetadata and carry internal artifact paths only until ArtifactManager registration. FakeImageProvider and FakeVideoProvider are deterministic defaults. ProviderFactory rejects non-Fake environment selections and any injected adapter marked paid while `ALLOW_PAID_PROVIDER` is false. |
| Image/video execution | `core/media_pipeline.py` | ImagePipeline and VideoPipeline share validation, workspace/mission output isolation, path-escape checks, usage normalization, safe PipelineResult conversion, and optional safe history recording. Video accepts only path-free, same-workspace artifact metadata as references. |
| YouTube and content E2E | `providers/content_media.py`, `core/content_orchestrator.py`, `core/execution_history.py` | YouTubeProvider defines title, description, tags, visibility, and artifact-reference upload contracts. FakeYouTubeProvider simulates uploads without OAuth, tokens, accounts, network access, or publication. ContentOrchestrator composes injected Music/Image/Video pipelines and the Fake upload boundary, stops after intermediate failure, returns safe stage summaries, and records safe stage/E2E history. |
| Scheduler | `core/scheduler.py` | InMemoryScheduler stores timezone-aware one-time or interval-recurring Schedule values and registered callbacks in process. Workspace-scoped operations, enabled state, an active-run guard, safe scalar metadata, missing-target failures, and injected SystemClock/FakeClock provide a deterministic contract. It is not durable and does not integrate cron, Windows Task Scheduler, Celery, or a queue. |
| Retry and recovery | `core/retry_recovery.py`, `core/content_orchestrator.py` | RetryExecutor applies RetryPolicy limits and exposes RetryState with attempt, category, safe error, retryability, and next backoff time. Timeout/transient-provider/history categories retry; validation/workspace/cost/authentication categories do not. ContentOrchestrator validates prior-result workspace ownership, reuses path-free artifacts from successful stages, and resumes at the failed stage. It does not interrupt calls or provide distributed recovery. |
| Personal AICompany composition | `application/personal_ai_company.py` | PersonalAICompany creates a Mission, runs CollaborationOrchestrator, then executes the Fake Content flow through RetryExecutor immediately or through an InMemoryScheduler target. Results contain safe Mission/Worker/stage/retry summaries while artifacts and ExecutionHistory remain on existing boundaries. This completes the offline personal composition baseline; persistence and Mission 91+ SaaS infrastructure are absent. |
| Versioned local persistence | `core/persistence.py` | StateRepository defines workspace-scoped save/get/list operations. InMemoryStateRepository is ephemeral; JsonStateRepository atomically stores a schema-versioned local JSON envelope at an injected path and safely ignores corrupt, incomplete, or unsupported-version records. PersistenceService stores redacted Mission summaries, Schedule, RetryState, and history summaries. Sensitive key families are removed recursively. No external database or migration runner is present. |
| Persistent artifact metadata | `core/artifact_manager.py`, `core/artifact_repository.py` | Existing artifact abstractions now include optional Mission, producer-stage, and availability fields. FileArtifactRepository can be configured with a storage root; then only a relative `internal_ref` is persisted, traversal/corrupt metadata is rejected, and missing files report MISSING. ArtifactManager queries by Workspace/Mission/ID. Explicit deletion removes metadata only and never deletes the underlying file. Legacy path behavior remains when no storage root is configured. |
| Persistent queue | `core/task_queue.py` | PersistentJobQueue reuses StateRepository for workspace/Mission-linked Jobs with PENDING/RUNNING/COMPLETED/FAILED state, idempotency keys, claim ownership, safe result summaries, RetryState, and restart recovery. A restored abandoned RUNNING Job becomes PENDING. InProcessJobWorker executes injected Fake/local targets deterministically; no broker or distributed lease exists. Existing TaskQueue remains compatible. |
| Batch execution | `core/batch.py` | BatchManager creates bounded, idempotent workspace-scoped groups of Jobs using PersistentJobQueue and StateRepository. Status is derived from item state; completed items remain intact when another item fails, failed retryable Jobs can be requeued, and summaries expose only safe Job result/retry metadata. |
| Read-only monitoring | `core/monitor.py`, `core/persistence.py` | WorkspaceMonitor reads existing StateRepository, PersistentJobQueue, Scheduler, ArtifactManager, ExecutionHistory, and an optional DepartmentManager boundary and returns safe Snapshot dictionaries. It observes Missions, enabled/disabled Schedules and Departments, Job counts and retry waiting, Batch progress/partial failure, Pipeline history, Usage, and Artifact AVAILABLE/MISSING state. Workspace summary becomes DEGRADED for missing artifacts or partial batches and never treats RUNNING as success. Missing usage fields are omitted rather than invented. Recursive read redaction removes sensitive keys and absolute paths while retaining standard token counts. Repository failures become type-only MonitorError results. Monitor performs no control action or persistence write; dashboards, APM, and real-time transport are absent. |
| Structured operational logging | `core/structured_logging.py`, `scripts/logger.py` | LogEvent is the safe operational/diagnostic contract and is intentionally separate from ExecutionHistory user records and Monitor state views. StructuredLogger contains write/read failures, defaults to INFO, normalizes errors to safe categories, preserves only present usage fields, and recursively strips prompt/objective/input/credential/header/cookie/raw-response/stack/environment metadata plus absolute paths. InMemoryLogger is deterministic; LocalFileLogger appends one sanitized JSON object per line and safely ignores corrupt lines after restart. Workspace-scoped queries filter component, exact level, timezone-aware range, and recent count. Manager Pipeline lifecycle, PersistentJobQueue, RetryExecutor, and ProviderFactory paid-policy decisions accept it through optional DI. The legacy `log()` function delegates to the local implementation. There is no external service, remote transport, distributed tracing, dashboard, rotation, retention, or automatic deletion. |
| Usage engine | `core/usage_engine.py`, `providers/models.py`, `core/persistence.py` | UsageEngine is a durable operational usage ledger, distinct from HistoryAnalyzer's derived execution-history report. It accepts the existing UsageMetadata object or partial dictionaries, preserves only present provider/model/input/output/total/cost fields, and ties each record to existing Workspace/Mission/execution identifiers. StateRepository supplies in-memory or JSON restart persistence and execution ID supplies the default idempotency key. Queries and summaries remain Workspace-scoped and may filter provider, model, timezone-aware range, and recent count. Corrupt records and repository failures are safely ignored or converted by `record_safe`; injected structured Logger failure never changes a successful record. No prompt, arbitrary metadata, path, provider response, API call, pricing table, credit, billing, or Settings contract is part of this layer. |
| Workspace settings | `core/settings_manager.py`, `config/settings.py`, `core/persistence.py` | SettingsManager adds a Workspace override layer without replacing safe process constants or creating another repository. WorkspaceSettings allowlists only offline provider names, bounded provider timeouts, Retry attempts/backoff, Batch item limits, structured log level, and the immutable false paid-provider flag. Updates use optimistic revisions and Workspace-qualified storage IDs; StateRepository supplies in-memory/JSON restart behavior. Safe adapters produce the existing ProviderFactory environment dictionary and RetryPolicy. Invalid/corrupt records fall back to defaults, update failures return type-only errors, and injected Logger failure is isolated. Secrets, prompts, arbitrary metadata, paths, paid-provider activation, external configuration services, and Mission 99 department composition are excluded. |
| AI Departments | `core/department.py`, `core/collaboration_worker.py`, `core/persistence.py` | Department is a versioned Workspace-owned organization record containing a safe name/summary, one fixed department type, enabled state, existing Worker IDs, an optional member lead, and supported registered task types. DepartmentManager reuses StateRepository with Workspace-qualified IDs for in-memory/JSON restart recovery and provides create/query/update, enable/disable, Worker assignment/removal, lead selection, and task-type configuration. WorkerDirectory registers injected BaseWorker instances with Workspace and task capabilities; it neither implements Workers nor persists a duplicate Worker store. Default department definitions create records only when actual compatible Workers and task types exist. Cross-Workspace Workers, duplicate assignments, invalid leads, stale revisions, corrupt data, sensitive strings, and paths are rejected or ignored. Department workflow selection/execution remains Mission 100. |
| Department workflow integration | `core/department_workflow.py`, `core/collaboration_orchestrator.py`, `core/retry_recovery.py` | DepartmentSelector performs deterministic offline selection by explicit Department or registered task type, filtering disabled/empty/foreign/missing-Worker candidates and placing the lead first. DepartmentWorkflow resolves existing Worker instances from WorkerDirectory, creates the existing CollaborationOrchestrator through DI, and invokes an injected Pipeline executor only after collaboration succeeds. It returns a safe PipelineResult and composes existing Settings RetryPolicy, Retry/Recovery, ExecutionHistory, structured Logging, UsageEngine, and path-free same-Workspace Artifact metadata. Transient Pipeline errors may retry; Worker/pipeline/workspace failures use safe categories; History/Logger/Usage failures are isolated. This is the verified Mission 100 Personal Operating System checkpoint scope, not a general OS agent, LLM organization designer, HR system, UI, or Mission 101 backend. |
| Creative text provider and pipeline | `providers/text.py`, `providers/factory.py`, `core/text_creation_pipeline.py` | TextProvider defines service-neutral requests/results for four bounded creative task types. FakeTextProvider is deterministic and free. OllamaTextProvider is explicit opt-in, loopback-only, model-required, credential-free, and transport-injectable; it is never the automatic fallback. It sends a task-specific JSON shape, and TextCreationPipeline normalizes structurally nonconforming Ollama text into an internal task contract without silently switching providers. The Pipeline rejects request echoes, sensitive keys, paths, and paid usage, then reuses PipelineResult, optional UsageMetadata, ArtifactManager, ExecutionHistory, and structured Logging. UTF-8 artifact bodies remain internal; external metadata is path-free and Workspace/Mission-scoped. The real loopback adapter was verified with Ollama 0.32.5 and `qwen2.5:1.5b`; automated tests remain transport-injected and offline. |
| Backend application foundation | `application/backend.py`, `api/app.py` | Mission 101 adds an immutable BackendDependencies composition boundary over the existing FastAPI and application-service layers. Routes do not create Pipelines or Providers. BackendHealthService is a read-only injected-probe aggregator for persistence, queue, monitor, schema version, and paid-provider-disabled state. Missing probes are distinct from failures; probe and Logger exceptions become safe degraded state. App instances receive independent injected services, while prior task, Workspace, User, Auth, membership, audit, and correlation behavior remains compatible. TestClient is the current verified HTTP runtime; an external ASGI server/deployment layer and Mission 102+ are not included. |
| Hybrid creative demo | `application/creative_demo.py`, `main.py` | HybridCreativeDemo creates one Mission, selects the persisted Content Department through DepartmentWorkflow, generates lyrics and a content plan, then invokes the existing Fake music/image/video/YouTube ContentOrchestrator. Its composition reuses JSON StateRepository, FileArtifactRepository, SettingsManager, UsageEngine, History, and local structured Logging. Default execution is fully Fake/Offline; `--local-text` only enables the explicit local Ollama text boundary while all media stages remain Fake. |

## Current pipeline behavior

- FILE: organizes known file types in a configured folder.
- MUSIC: uses an injected provider-neutral music generator, with an offline
  FakeMusicProvider default, and returns workspace-scoped safe artifact
  metadata.
- IMAGE: uses FakeImageProvider by default and returns only workspace-scoped
  safe artifact metadata.
- VIDEO: uses FakeVideoProvider by default and validates safe, same-workspace
  input artifact references.
- CONTENT E2E: composes MUSIC, IMAGE, VIDEO, and simulated YouTube upload with
  no external calls. The existing local ContentPipeline remains compatible.
- PERSONAL: composes collaboration and content generation for immediate or
  in-memory scheduled execution with bounded retry and same-workspace recovery.
- CONTENT: creates a local content-project scaffold with a review checklist;
  content type, title prefix, and tags can be supplied through validated Task
  parameters.
- RESEARCH: creates a structured local research-project scaffold with a review
  checklist and preserves validated local source records; research type and
  questions can be supplied through validated Task parameters. It does not
  perform web search or AI/API calls.
- HISTORY: reads recent execution records.
- FAIL: deliberately returns `FAILED` to validate failure handling.

`StubPipeline` remains as a reusable implementation for future unavailable
capabilities, but it is not registered by the current default registry.

## Planned extension boundaries

```text
UI / CLI ─┐
API layer ├→ Goal intake & planning → existing Task/Queue/Worker flow
          │                              ↓
          └────────────────────────→ PipelineRegistry → provider adapters

Storage layer: durable queue, project artifacts, execution history, settings
AI provider layer: provider adapters used by pipelines, never directly by UI
```

- UI/API should submit validated goals and query job/artifact state; they should
  not call filesystem pipelines directly.
- AI providers should be adapter implementations injected into pipelines so
  offline/local behavior stays testable.
- Storage should replace or extend JSON history with durable job, artifact, and
  queue records without changing Task/PipelineResult semantics.

## SaaS extension boundaries

- AI providers are injected adapters. They receive a standard request and
  return a standard response with usage metadata; Pipelines must not contain a
  provider name, model name, or credential.
- Persistent storage will attach workspace ownership to Tasks, artifacts,
  execution history, provider usage, and costs before any web-facing API is
  introduced.
- User identity is currently a minimal, credential-free domain. Membership,
  authorization, and authentication must build on it without adding secrets to
  its repository records. The existing membership roles are not yet enforced
  by API requests because authentication is not implemented.
- Supplying `workspace_id` when reading a Task provides data-scope mismatch
  protection at the current API boundary. It is not a substitute for future
  authenticated membership authorization.
- `create_app(auth_required=True)` enables Bearer checks for workspace/task
  access. MEMBER may access workspace work; OWNER and ADMIN may change
  membership. The default remains false solely for legacy API compatibility.
- Backend API and web dashboard layers will access workspace-scoped services,
  never filesystem Pipelines or provider credentials directly.
- Authentication, subscription, credit, and payment components belong outside
  the current Pipeline contract and will be added only after storage and API
  boundaries exist.
